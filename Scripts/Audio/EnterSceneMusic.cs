using UnityEngine;

public class EnterSceneMusic : MonoBehaviour
{
    bool isPlay = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (isPlay == false) 
        //{
        //    AudioManager.Instance().MusicPlay("1");
        //    isPlay = true;  
        //}
    }
}
